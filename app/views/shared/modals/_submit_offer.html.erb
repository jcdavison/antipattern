<div class="modal-content">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" ng-click="cancel()"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
    <span class='modal-title blue medium'>This is how it works:</span>
    <h4 class='modal-title red' ng-show='articleSubmitError()'>There was a problem, please try again.</h4>
  </div>
  <div class="modal-body clearfix">
    <div class="medium-small text-right" ng-show="display_status == 'instructions'">
      <div>You submit an offer to give a code review.</div>
      <div>The owner of the code review accepts or declines.</div>
      <div>You receive of notication of acceptance/decline.</div>
      <div>You give a solid code review.</div>
      <div>You get paid the posted value of the code review.</div>
    </div>
    <div class="medium-small text-right" ng-show="display_status == 'confirmation'">
      <div>Your offer has been received.</div>
      <div>The owner must now accept or decline</div>
      <div>You will receive email notification of their choice</div>
      <h3><a href="mailto:jd@startuplandia.io">email antipatten.io for help</a></h3>
    </div>
    <div class="medium-small text-right" ng-show="display_status == 'error'">
      there was a serious problem, with the api, it is the architect's fault
      <h3><a href="mailto:jd@startuplandia.io">email antipatten.io for help</a></h3>
    </div>
    <div ng-show="display_status == 'instructions'">
      <form id="offerCodeReview">
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
              <button class='btn btn-default tools accept pull-right' ng-click='offerCodeReview()'>Offer Code Review</button>
              <button class='btn btn-default tools cancel pull-right' ng-click='cancel()'>No Thanks</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
